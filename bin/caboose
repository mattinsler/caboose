#!/usr/bin/env node

process.env.NODE_PATH = [process.cwd() + '/node_modules'].concat(!process.env.NODE_PATH ? [] : process.env.NODE_PATH.split(':')).join(':');

require('coffee-script');
require('../index').cli.exec();
